---
phase: 06-distribution-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .flox/env.json
autonomous: false
requirements:
  - DIST-02
  - DIST-03
  - DIST-05

must_haves:
  truths:
    - "flox build sandflox produces a binary in result-sandflox/bin/sandflox"
    - "flox publish sandflox uploads the package to FloxHub under 8BitTacoSupreme catalog"
    - "flox install 8BitTacoSupreme/sandflox in a fresh environment makes sandflox available on PATH"
    - "The Nix expression uses lib.fileset.toSource and -trimpath for hermetic reproducible builds"
  artifacts:
    - path: ".flox/pkgs/sandflox.nix"
      provides: "Nix build expression with buildGoModule"
      contains: "fileset.toSource"
    - path: ".flox/env.json"
      provides: "Local path environment pointer"
      contains: '"name": "sandflox"'
  key_links:
    - from: ".flox/pkgs/sandflox.nix"
      to: "flox build sandflox"
      via: "Flox discovers nix expressions in .flox/pkgs/"
      pattern: "buildGoModule"
    - from: "flox build output"
      to: "flox publish"
      via: "publish uses built derivation from .flox/pkgs/"
      pattern: "flox publish sandflox"
    - from: "flox publish"
      to: "flox install 8BitTacoSupreme/sandflox"
      via: "FloxHub catalog serves published packages"
      pattern: "8BitTacoSupreme/sandflox"

user_setup:
  - service: git-remote
    why: "flox publish requires current commit to exist on remote"
    env_vars: []
    dashboard_config:
      - task: "Ensure git push permissions to origin/main"
        location: "GitHub repo settings or SSH key config"
---

<objective>
Publish sandflox to FloxHub and verify installation in a fresh Flox environment.

Purpose: Complete the distribution pipeline -- fix the env.json blocker, push all commits to origin, build the package, publish to FloxHub, and verify `flox install` works end-to-end. DIST-05 (hermetic Nix expression) is already satisfied from Phase 1 and will be verified by grep.

Output: sandflox available in FloxHub catalog as 8BitTacoSupreme/sandflox, installable via `flox install`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution-and-polish/06-RESEARCH.md
@.flox/pkgs/sandflox.nix
@.flox/env.json
@.flox/env/manifest.toml

<interfaces>
<!-- Existing Nix expression (already complete from Phase 1) -->
From .flox/pkgs/sandflox.nix:
```nix
{ buildGoModule, lib }:

buildGoModule {
  pname = "sandflox";
  version = "0.1.0";
  src = lib.fileset.toSource {
    root = ../../.;
    fileset = lib.fileset.unions [
      (lib.fileset.fileFilter (file: lib.hasSuffix ".go" file.name) ../../.)
      ../../go.mod
    ];
  };
  vendorHash = null;
  env.CGO_ENABLED = "0";
  buildFlags = [ "-trimpath" ];
  ldflags = [ "-s" "-w" "-X main.Version=0.1.0" ];
  doCheck = false;
  meta = with lib; {
    description = "macOS-native sandbox for AI coding agents";
    license = licenses.mit;
    platforms = platforms.darwin;
  };
}
```

Current .flox/env.json (BROKEN -- FloxHub-linked):
```json
{"owner":"8BitTacoSupreme","name":"sandflox","floxhub_url":"https://hub.flox.dev/","version":1}
```

Required .flox/env.json (committed state -- local path):
```json
{
  "name": "sandflox",
  "version": 1
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix env.json, push to remote, build and publish to FloxHub</name>
  <files>.flox/env.json</files>
  <read_first>
    - .flox/env.json (current broken state with "owner" field)
    - .flox/pkgs/sandflox.nix (verify lib.fileset.toSource and -trimpath present)
    - .flox/env/manifest.toml (confirm minimal manifest -- only go in [install])
    - .planning/phases/06-distribution-and-polish/06-RESEARCH.md (pitfalls and workflow)
  </read_first>
  <action>
    Execute the build-and-publish workflow in order:

    1. **Verify DIST-05 precondition** (already satisfied from Phase 1):
       Run `grep -q 'fileset.toSource' .flox/pkgs/sandflox.nix && grep -q 'trimpath' .flox/pkgs/sandflox.nix` to confirm hermetic build expression.

    2. **Fix env.json blocker** (Pitfall 1 from research):
       Run `git restore .flox/env.json` to revert to committed local path format: `{"name": "sandflox", "version": 1}`.
       Verify the restored file does NOT contain `"owner"` field: `grep -c '"owner"' .flox/env.json` must output `0`.

    3. **Clean up FloxHub artifact**:
       Run `rm -f .flox/env.lock` to remove the untracked FloxHub lockfile (not to be confused with `.flox/env/manifest.lock` which IS committed).

    4. **Push all commits to remote** (Pitfall 2 from research):
       Run `git push origin main`. Currently 48 commits ahead of origin/main.
       `flox publish` clones the repo from remote and does a clean build -- the current commit MUST exist on the remote.

    5. **Build the package**:
       Run `flox build sandflox`.
       Expected output: `result-sandflox` symlink pointing to `/nix/store/...-sandflox-0.1.0`.
       Verify binary exists: `ls -la result-sandflox/bin/sandflox`.
       Run `./result-sandflox/bin/sandflox --help` to confirm the binary is functional.

    6. **Publish to FloxHub**:
       Run `flox publish sandflox`.
       Expected: package uploaded as `8BitTacoSupreme/sandflox`.
       If timeout occurs (Pitfall 4), retry once -- the closure is small (zero runtime deps, CGO_ENABLED=0 static binary).

    7. **Verify package appears in catalog**:
       Run `flox search sandflox` and confirm output includes `8BitTacoSupreme/sandflox`.

    Note: Do NOT add a `[build.sandflox]` entry to manifest.toml -- `.flox/pkgs/sandflox.nix` handles the build (anti-pattern from research).
  </action>
  <verify>
    <automated>grep -q 'fileset.toSource' .flox/pkgs/sandflox.nix && grep -q 'trimpath' .flox/pkgs/sandflox.nix && echo "DIST-05 verified" && test -f result-sandflox/bin/sandflox && echo "build verified"</automated>
  </verify>
  <acceptance_criteria>
    - `.flox/env.json` contains `"name": "sandflox"` and does NOT contain `"owner"`
    - `.flox/env.lock` does not exist
    - `git log origin/main..HEAD --oneline | wc -l` outputs `0` (all commits pushed)
    - `result-sandflox/bin/sandflox` exists and is executable
    - `flox publish sandflox` exits 0
    - `.flox/pkgs/sandflox.nix` contains `fileset.toSource` (DIST-05)
    - `.flox/pkgs/sandflox.nix` contains `trimpath` (DIST-05)
  </acceptance_criteria>
  <done>
    sandflox is built via `flox build sandflox` and published to FloxHub via `flox publish sandflox`. DIST-02 satisfied. DIST-05 verified by grep.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify flox install in fresh environment</name>
  <files>N/A -- verification-only checkpoint, no files modified</files>
  <read_first>
    - .planning/phases/06-distribution-and-polish/06-RESEARCH.md (install verification workflow, Pitfall 6)
  </read_first>
  <action>
    Human verification checkpoint. Claude presents the install verification steps below.
    The user must test `flox install 8BitTacoSupreme/sandflox` in a fresh Flox environment to confirm DIST-03.

    Steps for the user:
    1. Open a new terminal (outside the sandflox project directory).
    2. Create a temporary test environment:
       cd /tmp && mkdir sandflox-install-test && cd sandflox-install-test && flox init
    3. Install the published package:
       flox install 8BitTacoSupreme/sandflox
    4. Verify the binary is available:
       flox activate -- sandflox --help
    5. (Optional) Quick functional check:
       cp /Users/jhogan/sandflox/policy.toml . && flox activate -- sandflox validate -policy policy.toml
    6. Cleanup:
       cd /tmp && rm -rf sandflox-install-test
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify -- user confirms flox install works"</automated>
  </verify>
  <acceptance_criteria>
    - `flox install 8BitTacoSupreme/sandflox` exits 0
    - `flox activate -- sandflox --help` outputs usage information
    - `flox activate -- which sandflox` shows a path containing `/nix/store/`
  </acceptance_criteria>
  <done>
    DIST-03 satisfied: sandflox is installable via `flox install 8BitTacoSupreme/sandflox` and functional in a fresh Flox environment.
  </done>
</task>

</tasks>

<verification>
All three phase requirements verified:
- DIST-02: `flox publish sandflox` exits 0, package appears in `flox search sandflox`
- DIST-03: `flox install 8BitTacoSupreme/sandflox` works in fresh env, binary runs
- DIST-05: `grep -q 'fileset.toSource' .flox/pkgs/sandflox.nix && grep -q 'trimpath' .flox/pkgs/sandflox.nix` both succeed
</verification>

<success_criteria>
1. `.flox/env.json` is a local path environment (no "owner" field)
2. All commits pushed to origin/main
3. `flox build sandflox` produces a working binary at `result-sandflox/bin/sandflox`
4. `flox publish sandflox` uploads successfully to FloxHub
5. `flox install 8BitTacoSupreme/sandflox` installs the binary in a fresh Flox environment
6. The installed `sandflox` binary is functional (--help, validate subcommand work)
7. `.flox/pkgs/sandflox.nix` contains `fileset.toSource` and `trimpath`
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-and-polish/06-01-SUMMARY.md`
</output>
