schema-version = "1.10.0"

# sandflox — minimal build manifest
# The Go binary handles all enforcement.
# This manifest provides the Go compiler for development.

[install]
go.pkg-path = "go"

[vars]

[hook]

[profile]

[services]
[include]
[build]
[options]
